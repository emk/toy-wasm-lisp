;; Top-level file for the toy Lisp runtime.
;;
;; Our job here is to include all the other files, using the %INCLUDE
;; WAT macro.
(module $toy_lisp_runtime.wasm
  (%include "runtime/watm/system.watm")
  (%include "runtime/watm/memory.watm")
  (%include "runtime/watm/types.watm")
  (%include "runtime/watm/start.watm"))
